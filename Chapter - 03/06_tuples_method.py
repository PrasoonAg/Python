#=========================================================================#
# 06 - Tuple Method
#=========================================================================#
# Index
tup = (1,2,3,4)
print(tup.index(4))
#returns index of first occurances

#=========================================================================#
# Count
#=========================================================================#
print(tup.count(2))
#counts total occurances
#=========================================================================#