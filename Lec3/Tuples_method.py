#06
#=========================================================================#
#Method
#=========================================================================#
#index
tup = (1,2,3,4)
print(tup.index(4))
#returns index of first occurances
#=========================================================================#
#count
print(tup.count(2))
#counts total occurances
#=========================================================================#